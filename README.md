# Fraud-Detection-System-of-Credit-Card-transitions
Built a fraud detection system using supervised machine learning on financial data. Applied data preprocessing, one-hot encoding, feature scaling, and SMOTE to handle class imbalance. Trained a Logistic Regression model and evaluated performance using F1-score and ROC-AUC.
